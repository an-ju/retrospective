<%= form_with(model: waste, local: true) do |form| %>
  <% if waste.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(waste.errors.count, "error") %> prohibited this waste from being saved:</h2>

      <ul>
      <% waste.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :waste_type %>
    <%= form.text_field :waste_type %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
