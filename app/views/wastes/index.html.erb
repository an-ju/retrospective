<p id="notice"><%= notice %></p>

<h1 class="text-indigo">Wastes</h1>

<%- @wastes.sort_by { |k, v| -seriousness(v) }.each do |waste_type, items| %>
  <%= render 'waste_item', waste_type: waste_type, items: items %>
<% end %>

<br>

<%= link_to 'New Waste', new_waste_path %>
